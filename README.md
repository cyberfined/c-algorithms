# c-algorithms
Set of algorithms and data structures implementations

# Build
```bash
make build
cd build
cmake ..
cmake --build .
cmake --build . --target test
```

# Content
* data structures
    * priority queues
        * binary heap
